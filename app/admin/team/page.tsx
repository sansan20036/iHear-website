"use client";

import { useEffect, useMemo, useRef, useState } from "react";
import { singleImageFromTransfer, transferHasFiles } from "../../../assets/image-intake";
import { hasRiskyEnglish } from "../../../assets/text-language-guard";
import { adminFetch, displayError } from "../admin-api";
import { useAdmin } from "../admin-context";
import { LanguageGuardNotice } from "../language-guard";
import { sortTeamProfilesByFirstName } from "../../../lib/team-sort";

type L10n={en:string;zhHant:string;zhHans:string};
type Profile=Record<string,any>&{id:string;personId:string;name:string;initials:string;section:"leader"|"tutor";status:"draft"|"published";profileVersion:number;personVersion:number;sortOrder:number;role:L10n;summary:L10n;bio:L10n};
const blankL10n=():L10n=>({en:"",zhHant:"",zhHans:""});
const blank=()=>({name:"",initials:"",section:"tutor",status:"draft",school:"",grade:"",showSchool:false,showGrade:false,consentConfirmed:false,role:blankL10n(),schoolDisplay:blankL10n(),languages:blankL10n(),strengths:blankL10n(),summary:blankL10n(),bio:blankL10n(),hobbies:blankL10n(),sortOrder:0});
const translatedFields=["role","summary","bio","languages","strengths","hobbies","schoolDisplay"];
const copy={en:{title:"Team roster",intro:"Add, edit, publish and safely reorder leaders and tutors.",add:"Add person",search:"Search name, school, grade or text",all:"All groups",leaders:"Leadership",tutors:"Tutors",allStatus:"All statuses",draft:"Draft",published:"Published",edit:"Edit",trash:"Move to trash",empty:"No matching people.",filteredOrder:"Clear search and filters before changing order.",formNew:"Add team member",formEdit:"Edit team member",name:"Name",initials:"Initials",section:"Group",school:"School",grade:"Grade",showSchool:"Show school publicly",showGrade:"Show grade publicly",consent:"Publication consent is confirmed",role:"Role / title",summary:"Card summary",bio:"Biography",languages:"Languages",strengths:"Strengths",hobbies:"Hobbies",schoolDisplay:"Public school wording",cancel:"Cancel",save:"Save draft",publish:"Save and publish",saving:"Saving…",avatar:"Profile photo",choose:"Choose photo",photoHelp:"PNG, JPEG or WebP up to 20MB; optimized to 800px / 500KB before upload.",conflict:"A newer roster is available. Finish or cancel this draft to refresh.",up:"Move up",down:"Move down",validationTitle:"Please complete these fields",requiredHint:"Fields marked * are required for this action.",recordVersion:"Latest record version",english:"English",hant:"Traditional Chinese",hans:"Simplified Chinese"},zhHant:{title:"團隊名冊",intro:"新增、修改、發布並安全調整核心團隊與小老師順序。",add:"新增人員",search:"搜尋姓名、學校、年級或內容",all:"全部分類",leaders:"核心團隊",tutors:"小老師",allStatus:"全部狀態",draft:"草稿",published:"已發布",edit:"編輯",trash:"移至回收區",empty:"沒有符合條件的人員。",filteredOrder:"請先清除搜尋與篩選，再調整完整名冊順序。",formNew:"新增團隊人員",formEdit:"編輯團隊人員",name:"姓名",initials:"姓名縮寫",section:"分類",school:"學校",grade:"年級",showSchool:"在公開頁顯示學校",showGrade:"在公開頁顯示年級",consent:"已確認取得公開同意",role:"職稱／角色",summary:"卡片簡介",bio:"完整介紹",languages:"語言",strengths:"專長",hobbies:"興趣",schoolDisplay:"公開頁學校文字",cancel:"取消",save:"儲存草稿",publish:"儲存並發布",saving:"儲存中…",avatar:"頭像照片",choose:"選擇照片",photoHelp:"PNG、JPEG 或 WebP，原檔最大 20MB；上傳前會縮至 800px／500KB。",conflict:"已有較新的名冊。請先完成或取消草稿再重新載入。",up:"上移",down:"下移",validationTitle:"請完成以下欄位",requiredHint:"標示 * 的欄位是本次操作的必填項目。",recordVersion:"最新資料版本",english:"英文",hant:"繁體中文",hans:"簡體中文"},zhHans:{title:"团队名册",intro:"新增、修改、发布并安全调整核心团队与小老师顺序。",add:"新增人员",search:"搜索姓名、学校、年级或内容",all:"全部分类",leaders:"核心团队",tutors:"小老师",allStatus:"全部状态",draft:"草稿",published:"已发布",edit:"编辑",trash:"移至回收区",empty:"没有符合条件的人员。",filteredOrder:"请先清除搜索与筛选，再调整完整名册顺序。",formNew:"新增团队人员",formEdit:"编辑团队人员",name:"姓名",initials:"姓名缩写",section:"分类",school:"学校",grade:"年级",showSchool:"在公开页显示学校",showGrade:"在公开页显示年级",consent:"已确认取得公开同意",role:"职称／角色",summary:"卡片简介",bio:"完整介绍",languages:"语言",strengths:"专长",hobbies:"兴趣",schoolDisplay:"公开页学校文字",cancel:"取消",save:"保存草稿",publish:"保存并发布",saving:"保存中…",avatar:"头像照片",choose:"选择照片",photoHelp:"PNG、JPEG 或 WebP，原文件最大 20MB；上传前会缩至 800px／500KB。",conflict:"已有较新的名册。请先完成或取消草稿再重新加载。",up:"上移",down:"下移",validationTitle:"请完成以下字段",requiredHint:"标示 * 的字段是本次操作的必填项目。",recordVersion:"最新数据版本",english:"英文",hant:"繁體中文",hans:"简体中文"}};
const sortCopy={
 en:{sortAlphabetically:"Sort tutors A–Z",sortConfirm:"Sort all tutors alphabetically by first name? This will replace the current tutor order.",sortDone:"All tutors are now sorted A–Z by first name.",sortAlready:"Tutors are already sorted A–Z by first name."},
 zhHant:{sortAlphabetically:"小老師依名字 A–Z 排序",sortConfirm:"確定將所有小老師依英文名字 A–Z 排序嗎？這會取代目前的小老師順序。",sortDone:"所有小老師已依名字 A–Z 排序。",sortAlready:"小老師目前已經是名字 A–Z 順序。"},
 zhHans:{sortAlphabetically:"小老师依名字 A–Z 排序",sortConfirm:"确定将所有小老师依英文名字 A–Z 排序吗？这会取代目前的小老师顺序。",sortDone:"所有小老师已依名字 A–Z 排序。",sortAlready:"小老师目前已经是名字 A–Z 顺序。"},
};
const imageIntakeCopy={
 en:{prompt:"Drop a photo here or paste with Ctrl+V / Cmd+V",formats:"PNG, JPEG or WebP · one image at a time · up to 20MB",empty:"The clipboard does not contain an image. Copy one image and try again.",emptyDrop:"No usable image was found in that drop. Drop a PNG, JPEG, or WebP file.",multiple:"Please add one image at a time."},
 zhHant:{prompt:"拖曳照片到這裡，或按 Ctrl+V／Cmd+V 貼上",formats:"PNG、JPEG 或 WebP · 一次一張 · 最大 20MB",empty:"剪貼簿中沒有可使用的圖片，請先複製一張圖片再試一次。",emptyDrop:"拖入的內容沒有可使用的圖片，請拖入 PNG、JPEG 或 WebP 檔案。",multiple:"一次只能加入一張圖片。"},
 zhHans:{prompt:"拖曳照片到这里，或按 Ctrl+V／Cmd+V 粘贴",formats:"PNG、JPEG 或 WebP · 一次一张 · 最大 20MB",empty:"剪贴板中没有可使用的图片，请先复制一张图片再试一次。",emptyDrop:"拖入的内容没有可使用的图片，请拖入 PNG、JPEG 或 WebP 文件。",multiple:"一次只能添加一张图片。"},
};

const saveCopy={en:{photo:"Uploading photo…",photoFailed:"The person details were saved, but the profile photo could not be stored. Your photo draft is still here; please try saving again."},zhHant:{photo:"上傳照片中…",photoFailed:"人員資料已儲存，但頭像照片尚未成功。照片草稿仍保留在畫面中，請再按一次儲存重試。"},zhHans:{photo:"上传照片中…",photoFailed:"人员资料已保存，但头像照片尚未成功。照片草稿仍保留在画面中，请再按一次保存重试。"}};
const avatarCopy={
 en:{drag:"Drag the photo to choose what stays centered.",zoom:"Zoom",showAll:"Show full photo",fill:"Fill circle",remove:"Delete photo",removeSelected:"Remove selected photo",deleteConfirm:"Delete this saved profile photo? The person record will remain.",deleting:"Deleting photo…",deleted:"Photo deleted.",saved:"Photo and crop settings updated.",verifyFailed:"The server saved the photo, but the refreshed image could not be verified. Please reopen this person to check it.",fullPreview:"Full source photo"},
 zhHant:{drag:"拖曳照片可調整保留在中央的位置。",zoom:"放大／縮小",showAll:"顯示完整照片",fill:"填滿圓框",remove:"刪除照片",removeSelected:"移除所選照片",deleteConfirm:"確定刪除這張已儲存的頭像嗎？人員資料會保留。",deleting:"刪除照片中…",deleted:"照片已刪除。",saved:"照片與顯示範圍已更新。",verifyFailed:"伺服器已儲存照片，但無法確認重新載入的圖片。請重新開啟此人員檢查。",fullPreview:"完整原始照片"},
 zhHans:{drag:"拖动照片可调整保留在中央的位置。",zoom:"放大／缩小",showAll:"显示完整照片",fill:"填满圆框",remove:"删除照片",removeSelected:"移除所选照片",deleteConfirm:"确定删除这张已保存的头像吗？人员资料会保留。",deleting:"删除照片中…",deleted:"照片已删除。",saved:"照片与显示范围已更新。",verifyFailed:"服务器已保存照片，但无法确认重新加载的图片。请重新打开此人员检查。",fullPreview:"完整原始照片"},
};
const cropCopy={
 en:{cropExisting:"Crop current photo",cropDraft:"Cropped photo ready. Save the person to publish it.",cropUnavailable:"The crop editor is still loading. Please try again.",photoHelp:"Choose a group or portrait photo, then move and resize the circle around one person. Only the final square crop will be uploaded."},
 zhHant:{cropExisting:"裁切目前照片",cropDraft:"裁切後的照片已準備好，請儲存人員資料以發布。",cropUnavailable:"裁切工具仍在載入，請稍後再試。",photoHelp:"可選擇多人合照或個人照，再移動、縮放圓框圈選單一人物；只會上傳最後的方形裁切結果。"},
 zhHans:{cropExisting:"裁切目前照片",cropDraft:"裁切后的照片已准备好，请保存人员资料以发布。",cropUnavailable:"裁切工具仍在加载，请稍后再试。",photoHelp:"可选择多人合照或个人照，再移动、缩放圆框圈选单一人物；只会上传最后的方形裁切结果。"},
};
const translationCopy={
 en:{auto:"Automatically update Chinese translations",replace:"Also overwrite existing or manually edited Chinese",prepare:"Generate translation preview",preparing:"Translating…",ready:"Chinese preview ready. Review it, then save or publish.",manual:"Edit Chinese manually"},
 zhHant:{auto:"自動更新中文翻譯",replace:"同時覆蓋既有或人工修改過的中文",prepare:"產生翻譯預覽",preparing:"翻譯中…",ready:"中文預覽已完成，請檢查後再儲存或發布。",manual:"自行編輯中文"},
 zhHans:{auto:"自动更新中文翻译",replace:"同时覆盖现有或人工修改过的中文",prepare:"生成翻译预览",preparing:"翻译中…",ready:"中文预览已完成，请检查后再保存或发布。",manual:"自行编辑中文"},
};
type ValidationItem={key:string;label:string};
function languageLabel(key:string,text:any){return key==="en"?text.english:key==="zhHant"?text.hant:text.hans}
function issueLabel(key:string,text:any){const[base,language]=key.split(".");const labels:Record<string,string>={name:text.name,initials:text.initials,section:text.section,status:text.allStatus,school:text.school,grade:text.grade,consentConfirmed:text.consent,role:text.role,summary:text.summary,bio:text.bio,languages:text.languages,strengths:text.strengths,hobbies:text.hobbies,schoolDisplay:text.schoolDisplay,sortOrder:text.recordVersion,profileVersion:text.recordVersion,personVersion:text.recordVersion};const label=labels[base]||text.validationTitle;return language?`${languageLabel(language,text)} · ${label}`:label}
function requiredFields(form:any,publish:boolean,text:any,requireTranslations=false):ValidationItem[]{const result:ValidationItem[]=[];const add=(key:string)=>result.push({key,label:issueLabel(key,text)});if(!form.name?.trim())add("name");if(!form.initials?.trim())add("initials");if(form.showSchool&&!form.school?.trim())add("school");if(form.showGrade&&!form.grade?.trim())add("grade");if(publish){if(!form.consentConfirmed)add("consentConfirmed");const required=new Set(["role","bio",...(form.section==="tutor"?["summary"]:[])]);for(const field of translatedFields){const value=form[field]||{};const complete=required.has(field)||Boolean(value.en?.trim()||value.zhHant?.trim()||value.zhHans?.trim());if(!complete)continue;if(!value.en?.trim())add(`${field}.en`);if(requireTranslations&&!value.zhHant?.trim())add(`${field}.zhHant`);if(requireTranslations&&!value.zhHans?.trim())add(`${field}.zhHans`)}}return result}
function profileMatchesDraft(profile:any,input:any){const localized=(value:any)=>({en:String(value?.en||""),zhHant:String(value?.zhHant||""),zhHans:String(value?.zhHans||"")});const snapshot=(value:any)=>({name:String(value.name||""),initials:String(value.initials||"").toUpperCase(),section:value.section,status:value.status,school:String(value.school||""),grade:String(value.grade||""),showSchool:value.showSchool===true,showGrade:value.showGrade===true,consentConfirmed:value.consentConfirmed===true||Boolean(value.publicationConsentAt),role:localized(value.role),schoolDisplay:localized(value.schoolDisplay),languages:localized(value.languages),strengths:localized(value.strengths),summary:localized(value.summary),bio:localized(value.bio),hobbies:localized(value.hobbies)});return JSON.stringify(snapshot(profile))===JSON.stringify(snapshot(input))}
function generatedAvatarAlt(name:string):L10n{const person=String(name||"").trim()||"Team member";return{en:`Portrait of ${person}`,zhHant:`${person} 的個人頭像`,zhHans:`${person} 的个人头像`}}

export default function AdminTeamPage(){
 const adminContext=useAdmin();
 const{locale,setDirty,setSubmitting,submitting}=useAdmin();const cropText=cropCopy[locale];const translateText=translationCopy[locale];const intakeText=imageIntakeCopy[locale];const text={...copy[locale],...sortCopy[locale],photoHelp:cropText.photoHelp};const avatarText={...avatarCopy[locale],get fill(){return media?.[avatarSlot(editing)]?cropText.cropExisting:""}};const[profiles,setProfiles]=useState<Profile[]>([]);const[media,setMedia]=useState<Record<string,any>>({});const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[notice,setNotice]=useState("");const[query,setQuery]=useState("");const[section,setSection]=useState("all");const[status,setStatus]=useState("all");const[editing,setEditing]=useState<Profile|null|"new">(null);const[form,setForm]=useState<any>(blank());const[tab,setTab]=useState<keyof L10n>("en");const[file,setFile]=useState<File|null>(null);const[preview,setPreview]=useState("");const[focal,setFocal]=useState("50,50");const[zoom,setZoom]=useState(100);const[photoNotice,setPhotoNotice]=useState("");const[photoDragActive,setPhotoDragActive]=useState(false);const[external,setExternal]=useState(false);const[validationMode,setValidationMode]=useState<null|"draft"|"published">(null);const[serverIssueKeys,setServerIssueKeys]=useState<string[]>([]);const[saveStage,setSaveStage]=useState<"details"|"photo"|"delete-photo"|null>(null);const[autoTranslate,setAutoTranslate]=useState(true);const[replaceTranslations,setReplaceTranslations]=useState(false);const[translationReceipt,setTranslationReceipt]=useState("");const[translating,setTranslating]=useState(false);const[translationReady,setTranslationReady]=useState(false);const[englishGuardAccepted,setEnglishGuardAccepted]=useState(false);const original=useRef("");const savingRef=useRef(false);const ownTeamRevision=useRef("");const pendingTeamRevision=useRef("");const cropImageRef=useRef<HTMLImageElement|null>(null);const autoTranslationAttempt=useRef("");
 const load=async()=>{const[d,m]=await Promise.all([adminFetch<any>("/api/team-profiles?includeDrafts=true"),adminFetch<any>("/api/site-media")]);const next=[...(d.leaders||[]),...(d.tutors||[])];setProfiles(next);setMedia(m.items||{});setLoading(false);return next};
 useEffect(()=>{load().catch(e=>{setError(displayError(e,locale));setLoading(false)})},[]);
 useEffect(()=>{let unregister:(()=>void)|undefined;const timer=setTimeout(()=>{const live=(window as any).iHearLiveContent;if(!live)return;unregister=live.register("team",{refresh:async(context?:{revision?:unknown})=>{const revision=String(context?.revision||"");if(savingRef.current){pendingTeamRevision.current=revision;return}if(revision&&revision===ownTeamRevision.current)return;if(editing){const latest=await load();if(editing!=="new"){const current=latest.find(item=>item.id===editing.id);if(!current||current.profileVersion!==editing.profileVersion||current.personVersion!==editing.personVersion)setExternal(true)}return}await load()}})},200);return()=>{clearTimeout(timer);unregister?.()}},[editing]);
 useEffect(()=>{const dirty=editing!==null&&(JSON.stringify({form,file:file?.name||"",focal,zoom})!==original.current||Boolean(translationReceipt));setDirty("team-form",dirty);return()=>setDirty("team-form",false)},[editing,form,file,focal,zoom,translationReceipt,setDirty]);
 useEffect(()=>{setServerIssueKeys([])},[form]);
 useEffect(()=>()=>{if(preview.startsWith("blob:"))URL.revokeObjectURL(preview)},[preview]);
 useEffect(()=>{const signature=englishTranslationSignature();if(!editing||!autoTranslate||submitting||translating||translationReady||!hasEnglishTranslationSource()||(hasRiskyEnglish(englishSourceValues())&&!englishGuardAccepted)||signature===autoTranslationAttempt.current)return;const timer=window.setTimeout(()=>{autoTranslationAttempt.current=signature;void prepareTranslations()},800);return()=>window.clearTimeout(timer)},[editing,autoTranslate,form,englishGuardAccepted,replaceTranslations,submitting,translating,translationReady]);
 const filtered=useMemo(()=>{const needle=query.trim().toLowerCase();return profiles.filter(p=>{if(section!=="all"&&p.section!==section)return false;if(status!=="all"&&p.status!==status)return false;const hay=[p.name,p.school,p.grade,p.section,p.status,...Object.values(p.role||{}),...Object.values(p.summary||{})].join(" ").toLowerCase();return !needle||hay.includes(needle)})},[profiles,query,section,status]);
 const filtering=Boolean(query.trim()||section!=="all"||status!=="all");
 const tutorCount=profiles.filter(profile=>profile.section==="tutor").length;
 function avatarSlot(profile?:Profile|null|"new"){return profile&&profile!=="new"?`team.${profile.personId.replace(/^person-/,"")}.avatar`:""}
 function englishTranslationSignature(source=form){return JSON.stringify({fields:Object.fromEntries(translatedFields.map(field=>[field,String(source[field]?.en||"")])),name:String(source.name||""),school:String(source.school||"")})}
 function englishSourceValues(source=form){return translatedFields.map(field=>String(source[field]?.en||""))}
 function hasEnglishTranslationSource(){return translatedFields.some(field=>String(form[field]?.en||"").trim())}
 function open(profile?:Profile){const next=profile?{...profile,consentConfirmed:Boolean(profile.publicationConsentAt)}:blank();setEditing(profile||"new");setForm(next);setTab("en");setFile(null);setValidationMode(null);setServerIssueKeys([]);setError("");setPhotoNotice("");setPhotoDragActive(false);setAutoTranslate(true);setReplaceTranslations(false);setTranslationReceipt("");setTranslationReady(false);setEnglishGuardAccepted(false);const item=media[avatarSlot(profile)];setPreview(item?.src||"");setFocal(`${item?.focalX??50},${item?.focalY??50}`);setZoom(item?.zoom??100);setExternal(false);autoTranslationAttempt.current=englishTranslationSignature(next);original.current=JSON.stringify({form:next,file:"",focal:`${item?.focalX??50},${item?.focalY??50}`,zoom:item?.zoom??100})}
 function close(){if(submitting||saveStage||translating)return;adminContext.confirmAction(()=>{setEditing(null);setFile(null);setValidationMode(null);setServerIssueKeys([]);setError("");setPhotoNotice("");setPhotoDragActive(false);setTranslationReceipt("");setTranslationReady(false);if(preview.startsWith("blob:"))URL.revokeObjectURL(preview);setPreview("")})}
 function loc(field:string,value:string){setForm((x:any)=>({...x,[field]:{...x[field],[tab]:value}}));if(tab==="en"){setTranslationReceipt("");setTranslationReady(false);setEnglishGuardAccepted(false)}}
 async function cropSource(source:File|string){setError("");setPhotoNotice("");try{const cropper=(window as any).iHearAvatarCropper;if(!cropper)throw new Error(cropText.cropUnavailable);const result=await cropper.open({source,locale});if(!result)return;if(preview.startsWith("blob:"))URL.revokeObjectURL(preview);setFile(result.file);setPreview(URL.createObjectURL(result.file));setFocal("50,50");setZoom(100);setPhotoNotice(cropText.cropDraft)}catch(e){setError(displayError(e,locale))}}
 async function pick(selected?:File){if(selected)await cropSource(selected)}
 async function receivePhotoTransfer(transfer:DataTransfer|null,fromClipboard=false){
  if(submitting||translating)return;
  const result=singleImageFromTransfer(transfer);
  if(result.status==="empty"){setError(fromClipboard?intakeText.empty:intakeText.emptyDrop);return}
  if(result.status==="multiple"){setError(intakeText.multiple);return}
  setError("");await pick(result.file||undefined);
 }
 async function cropExisting(){const slot=avatarSlot(editing);const item=media[slot];if(!item||submitting)return;await cropSource(`/api/site-media/${encodeURIComponent(slot)}/source?expectedVersion=${item.recordVersion}`)}
 function dragStart(){}
 function dragMove(){}
 function dragEnd(){}
 function showFullPhoto(){}
 function fillCircle(){cropExisting()}
 function validationFor(publish:boolean){return requiredFields(form,publish,text,publish&&(!autoTranslate||translationReady))}
 function removeSelected(){const item=media[avatarSlot(editing)];if(preview.startsWith("blob:"))URL.revokeObjectURL(preview);setFile(null);setPreview(item?.src||"");setFocal(`${item?.focalX??50},${item?.focalY??50}`);setZoom(item?.zoom??100);setPhotoNotice("")}
 async function deletePhoto(){const slot=avatarSlot(editing);const item=media[slot];if(file)return removeSelected();if(!slot||!item||submitting||!window.confirm(avatarText.deleteConfirm))return;setError("");setPhotoNotice("");setSaveStage("delete-photo");setSubmitting(true);try{const result=await adminFetch<any>(`/api/site-media/${encodeURIComponent(slot)}`,{method:"DELETE",body:JSON.stringify({expectedVersion:item.recordVersion})});setMedia(items=>{const next={...items};delete next[slot];return next});setPreview("");setFocal("50,50");setZoom(100);try{const prior=JSON.parse(original.current);original.current=JSON.stringify({...prior,file:"",focal:"50,50",zoom:100})}catch{};(window as any).iHearLiveContent?.announce("content",result.revision);setPhotoNotice(avatarText.deleted)}catch(e){setError(displayError(e,locale))}finally{setSaveStage(null);setSubmitting(false)}}
 function revealFirst(items:ValidationItem[]){const first=items[0];if(!first)return;const language=first.key.split(".")[1];if(language==="en"||language==="zhHant"||language==="zhHans")setTab(language);window.setTimeout(()=>{const target=document.querySelector(`[data-team-field="${first.key}"]`) as HTMLElement|null;target?.scrollIntoView({behavior:"smooth",block:"center"});target?.querySelector<HTMLElement>("input,textarea,select,button")?.focus()},80)}
 async function prepareTranslations(){
  if(submitting||translating||(hasRiskyEnglish(englishSourceValues())&&!englishGuardAccepted))return;
  autoTranslationAttempt.current=englishTranslationSignature();
  const localizedFields=Object.fromEntries(fields.filter(field=>Object.values(form[field]||{}).some(value=>String(value||"").trim())).map(field=>[field,form[field]]));
  setTranslating(true);setError("");setPhotoNotice("");
  try{
   let nextReceipt="";let nextForm=form;
   if(Object.keys(localizedFields).length){
    const result=await adminFetch<any>("/api/admin/translations/preview",{method:"POST",body:JSON.stringify({resource:{type:"team",scope:"",id:editing==="new"?"__new__":(editing as Profile).id,version:editing==="new"?undefined:(editing as Profile).profileVersion},fields:localizedFields,allowCjkEnglish:englishGuardAccepted,force:replaceTranslations?Object.fromEntries(Object.keys(localizedFields).map(field=>[field,["zhHant","zhHans"]])):{},personNames:[form.name].filter(Boolean),contextTerms:[form.school].filter(Boolean)})});
    nextForm={...form,...Object.fromEntries(Object.entries(result.fields).map(([field,value]:any)=>[field,value.value]))};nextReceipt=result.receipt;
   }
   setForm(nextForm);setTranslationReceipt(nextReceipt);setTranslationReady(true);setPhotoNotice(translateText.ready);
  }catch(e){setError(displayError(e,locale))}finally{setTranslating(false)}
 }
 async function save(publish:boolean){
  if(submitting||savingRef.current)return;
  const validation=validationFor(publish);
  setValidationMode(publish?"published":"draft");setServerIssueKeys([]);setError("");
  if(validation.length){revealFirst(validation);return}
  if(autoTranslate&&!translationReady){await prepareTranslations();return}
  let savedProfile:Profile|null=null;
  let rebasedForm:any=null;
  savingRef.current=true;pendingTeamRevision.current="";setSaveStage("details");setSubmitting(true);
  try{
   const body={...form,status:publish?"published":"draft",sortOrder:Number(form.sortOrder)||0,translationReceipt:autoTranslate&&translationReceipt?translationReceipt:undefined,...(editing!=="new"?{profileVersion:(editing as Profile).profileVersion,personVersion:(editing as Profile).personVersion}:{})};
   let data:any=null;
   if(external&&file){const latest=await adminFetch<any>("/api/team-profiles?includeDrafts=true");const candidates=[...(latest.leaders||[]),...(latest.tutors||[])];const recovered=editing==="new"?candidates.find(profile=>profileMatchesDraft(profile,body)):candidates.find(profile=>profile.id===(editing as Profile).id&&profileMatchesDraft(profile,body));if(recovered){data={profile:recovered,revision:null};setExternal(false)}}
   if(!data)data=editing==="new"?await adminFetch<any>("/api/team-profiles",{method:"POST",body:JSON.stringify(body)}):await adminFetch<any>(`/api/team-profiles/${(editing as Profile).id}`,{method:"PATCH",body:JSON.stringify(body)});
   savedProfile=data.profile as Profile;
   rebasedForm={...form,...savedProfile,consentConfirmed:Boolean(savedProfile.publicationConsentAt)};
   setForm(rebasedForm);setEditing(savedProfile);
   setProfiles(items=>items.some(item=>item.id===savedProfile?.id)?items.map(item=>item.id===savedProfile?.id?savedProfile as Profile:item):[...items,savedProfile as Profile]);
   ownTeamRevision.current=String(data.revision?.revision||data.revision||"");
   if(data.revision)(window as any).iHearLiveContent?.announce("team",data.revision);
    const slot=avatarSlot(savedProfile);const current=media[slot];const[x,y]=focal.split(",").map(Number);
    let verifyWarning="";
    if(file){
     setSaveStage("photo");
     const avatarAlt=generatedAvatarAlt(savedProfile.name);const fd=new FormData();fd.append("file",file,"avatar.webp");fd.append("altEn",avatarAlt.en);fd.append("altZhHant",avatarAlt.zhHant);fd.append("altZhHans",avatarAlt.zhHans);fd.append("focalX",String(x));fd.append("focalY",String(y));fd.append("zoom",String(zoom));fd.append("expectedVersion",String(current?.recordVersion||0));const uploaded=await adminFetch<any>(`/api/site-media/${encodeURIComponent(slot)}`,{method:"POST",body:fd});
     setMedia(items=>({...items,[slot]:uploaded.item}));(window as any).iHearLiveContent?.announce("content",uploaded.revision);
     try{const fresh=await adminFetch<any>(`/api/site-media?verify=${Date.now()}`);const verified=fresh.items?.[slot];if(!verified||verified.recordVersion!==uploaded.item.recordVersion||verified.src!==uploaded.item.src)verifyWarning=avatarText.verifyFailed;else setMedia(items=>({...items,[slot]:verified}))}catch{verifyWarning=avatarText.verifyFailed}
    }
    original.current=JSON.stringify({form:rebasedForm,file:"",focal,zoom});if(preview.startsWith("blob:"))URL.revokeObjectURL(preview);setEditing(null);setFile(null);setPreview("");setValidationMode(null);setPhotoNotice("");setTranslationReceipt("");setTranslationReady(false);if(verifyWarning)setError(verifyWarning);
  }catch(e:any){
   if(savedProfile){setError(saveCopy[locale].photoFailed)}
   else if(e?.issues){const keys=Object.keys(e.issues);setServerIssueKeys(keys);revealFirst(keys.map(key=>({key,label:issueLabel(key,text)})));}
   else setError(displayError(e,locale));
  }finally{
   savingRef.current=false;setSaveStage(null);setSubmitting(false);
   const pending=pendingTeamRevision.current;pendingTeamRevision.current="";
   if(pending&&pending!==ownTeamRevision.current)setExternal(true);
  }
 }
 async function trash(profile:Profile){if(submitting)return;setSubmitting(true);try{const d=await adminFetch<any>(`/api/team-profiles/${profile.id}`,{method:"DELETE",body:JSON.stringify({profileVersion:profile.profileVersion})});setProfiles(x=>x.filter(v=>v.id!==profile.id));(window as any).iHearLiveContent?.announce("team",d.revision)}catch(e){setError(displayError(e,locale))}finally{setSubmitting(false)}}
 async function move(profile:Profile,direction:number){if(filtering||submitting)return;const group=profiles.filter(p=>p.section===profile.section).sort((a,b)=>a.sortOrder-b.sortOrder);const i=group.findIndex(p=>p.id===profile.id),j=i+direction;if(j<0||j>=group.length)return;[group[i],group[j]]=[group[j],group[i]];const previous=profiles;setProfiles(x=>x.map(p=>{const index=group.findIndex(v=>v.id===p.id);return index<0?p:{...p,sortOrder:(index+1)*10}}));setSubmitting(true);try{const d=await adminFetch<any>("/api/team-profiles/reorder",{method:"PATCH",body:JSON.stringify({section:profile.section,ordered:group.map(p=>({id:p.id,version:p.profileVersion}))})});await load();(window as any).iHearLiveContent?.announce("team",d.revision)}catch(e){setProfiles(previous);setError(displayError(e,locale))}finally{setSubmitting(false)}}
 async function sortTutorsAlphabetically(){
  if(filtering||submitting)return;
  const current=profiles.filter(profile=>profile.section==="tutor").sort((a,b)=>a.sortOrder-b.sortOrder);
  const ordered=sortTeamProfilesByFirstName(current);
  if(ordered.length<2||ordered.every((profile,index)=>profile.id===current[index]?.id)){setNotice(text.sortAlready);return}
  if(!window.confirm(text.sortConfirm))return;
  const previous=profiles;
  setError("");setNotice("");
  setProfiles(items=>items.map(profile=>{const index=ordered.findIndex(item=>item.id===profile.id);return index<0?profile:{...profile,sortOrder:(index+1)*10}}));
  setSubmitting(true);
  try{
   const data=await adminFetch<any>("/api/team-profiles/reorder",{method:"PATCH",body:JSON.stringify({section:"tutor",ordered:ordered.map(profile=>({id:profile.id,version:profile.profileVersion}))})});
   await load();
   (window as any).iHearLiveContent?.announce("team",data.revision);
   setNotice(text.sortDone);
  }catch(e){setProfiles(previous);setError(displayError(e,locale))}
  finally{setSubmitting(false)}
 }
 const validationItems=useMemo(()=>{const combined=[...(validationMode?validationFor(validationMode==="published"):[]),...serverIssueKeys.map(key=>({key,label:issueLabel(key,text)}))];return combined.filter((item,index)=>combined.findIndex(other=>other.key===item.key)===index)},[validationMode,serverIssueKeys,form,locale]);
 const invalidKeys=new Set(validationItems.map(item=>item.key));
 const fields=translatedFields;
 const[focalX,focalY]=focal.split(",").map(Number);
 return <section className="admin-page">
  <header className="admin-page-head"><div><h1>{text.title}</h1><p>{text.intro}</p></div><button className="admin-button primary" onClick={()=>open()}>{text.add}</button></header>
  {error&&!editing&&<p className="admin-alert" role="alert">{error}</p>}
  {notice&&!editing&&<p className="admin-alert admin-success" role="status">{notice}</p>}
  <div className="admin-toolbar"><input className="admin-search" value={query} onChange={e=>setQuery(e.target.value)} placeholder={text.search}/><select className="admin-select" value={section} onChange={e=>setSection(e.target.value)}><option value="all">{text.all}</option><option value="leader">{text.leaders}</option><option value="tutor">{text.tutors}</option></select><select className="admin-select" value={status} onChange={e=>setStatus(e.target.value)}><option value="all">{text.allStatus}</option><option value="draft">{text.draft}</option><option value="published">{text.published}</option></select><button type="button" className="admin-button secondary" disabled={loading||filtering||submitting||tutorCount<2} title={filtering?text.filteredOrder:undefined} onClick={sortTutorsAlphabetically}>{text.sortAlphabetically} ({tutorCount})</button></div>
  {filtering&&<p className="admin-muted">{text.filteredOrder}</p>}
  {loading?<p>Loading…</p>:filtered.length?<div className="admin-card-grid">{filtered.map(p=><article className="admin-card" key={p.id}><div className="admin-card-head"><div><h2>{p.name}</h2><p>{p.role?.[locale]||p.role?.en||"—"}</p></div><span className={`admin-status ${p.status}`}>{p.status==="published"?text.published:text.draft}</span></div><p>{p.school}{p.grade?` · ${p.grade}`:""}</p><div className="admin-card-actions"><button className="admin-button secondary" onClick={()=>open(p)}>{text.edit}</button><button className="admin-button danger" onClick={()=>trash(p)}>{text.trash}</button><span className="admin-reorder"><button title={text.up} disabled={filtering||submitting} onClick={()=>move(p,-1)}>↑</button><button title={text.down} disabled={filtering||submitting} onClick={()=>move(p,1)}>↓</button></span></div></article>)}</div>:<div className="admin-empty">{text.empty}</div>}
  {editing&&<><button className="admin-drawer-backdrop" aria-label={text.cancel} onClick={close}/><aside className="admin-drawer" role="dialog" aria-modal="true" aria-labelledby="team-form-title" aria-busy={submitting||translating} onPaste={event=>{if(!transferHasFiles(event.clipboardData))return;event.preventDefault();void receivePhotoTransfer(event.clipboardData,true)}}>
   <div className="admin-drawer-head"><h2 id="team-form-title">{editing==="new"?text.formNew:text.formEdit}</h2><button className="admin-icon-button" disabled={submitting||translating} onClick={close} aria-label={text.cancel}>×</button></div>
   {external&&<p className="admin-alert">{text.conflict}</p>}{error&&<p className="admin-alert" role="alert">{error}</p>}
   {validationItems.length>0&&<div className="admin-alert admin-validation" role="alert"><strong>{text.validationTitle}</strong><ul>{validationItems.map(item=><li key={item.key}>{item.label}</li>)}</ul></div>}
   <div className="admin-form"><p className="admin-required-note">{text.requiredHint}</p>
    <div className="admin-form-grid"><label className="admin-field" data-team-field="name"><span>{text.name} *</span><input disabled={submitting||translating} aria-invalid={invalidKeys.has("name")} value={form.name} maxLength={160} onChange={e=>{setForm((x:any)=>({...x,name:e.target.value}));setTranslationReceipt("");setTranslationReady(false)}}/></label><label className="admin-field" data-team-field="initials"><span>{text.initials} *</span><input disabled={submitting||translating} aria-invalid={invalidKeys.has("initials")} value={form.initials} maxLength={8} onChange={e=>setForm((x:any)=>({...x,initials:e.target.value.toUpperCase()}))}/></label><label className="admin-field"><span>{text.section}</span><select disabled={submitting||translating} value={form.section} onChange={e=>setForm((x:any)=>({...x,section:e.target.value}))}><option value="leader">{text.leaders}</option><option value="tutor">{text.tutors}</option></select></label><label className="admin-field" data-team-field="school"><span>{text.school}{form.showSchool?" *":""}</span><input disabled={submitting||translating} aria-invalid={invalidKeys.has("school")} value={form.school} onChange={e=>{setForm((x:any)=>({...x,school:e.target.value}));setTranslationReceipt("");setTranslationReady(false)}}/></label><label className="admin-field" data-team-field="grade"><span>{text.grade}{form.showGrade?" *":""}</span><input disabled={submitting||translating} aria-invalid={invalidKeys.has("grade")} value={form.grade} onChange={e=>setForm((x:any)=>({...x,grade:e.target.value}))}/></label></div>
    <label className="admin-checkbox"><input type="checkbox" disabled={submitting||translating} checked={form.showSchool} onChange={e=>setForm((x:any)=>({...x,showSchool:e.target.checked}))}/>{text.showSchool}</label><label className="admin-checkbox"><input type="checkbox" disabled={submitting||translating} checked={form.showGrade} onChange={e=>setForm((x:any)=>({...x,showGrade:e.target.checked}))}/>{text.showGrade}</label><label className="admin-checkbox" data-team-field="consentConfirmed"><input aria-invalid={invalidKeys.has("consentConfirmed")} type="checkbox" disabled={submitting||translating} checked={form.consentConfirmed} onChange={e=>setForm((x:any)=>({...x,consentConfirmed:e.target.checked}))}/>{text.consent}{validationMode==="published"?" *":""}</label>
    <div className="admin-translation-controls"><label className="admin-checkbox"><input type="checkbox" checked={autoTranslate} disabled={submitting||translating} onChange={e=>{setAutoTranslate(e.target.checked);setTranslationReceipt("");setTranslationReady(false);autoTranslationAttempt.current=e.target.checked?"":englishTranslationSignature()}}/>{translateText.auto}</label>{autoTranslate&&<label className="admin-checkbox"><input type="checkbox" checked={replaceTranslations} disabled={submitting||translating} onChange={e=>{setReplaceTranslations(e.target.checked);setTranslationReceipt("");setTranslationReady(false);autoTranslationAttempt.current=""}}/>{translateText.replace}</label>}{translationReady&&<p className="admin-translation-status" role="status">{translateText.ready}</p>}</div>
    <div className="admin-locale-tabs">{(["en","zhHant","zhHans"] as (keyof L10n)[]).map(x=><button type="button" className={tab===x?"active":""} key={x} onClick={()=>setTab(x)}>{x==="en"?text.english:x==="zhHant"?text.hant:text.hans}</button>)}</div>
    {fields.map(field=>{const key=`${field}.${tab}`;const required=validationMode==="published"&&tab==="en"&&(field==="role"||field==="bio"||(field==="summary"&&form.section==="tutor"));return <div data-team-field={key} key={field}><label className="admin-field"><span>{(text as any)[field]}{required?" *":""}</span>{["bio","summary","strengths","hobbies"].includes(field)?<textarea disabled={submitting||translating} aria-invalid={invalidKeys.has(key)} value={form[field]?.[tab]||""} onChange={e=>loc(field,e.target.value)}/>:<input disabled={submitting||translating} aria-invalid={invalidKeys.has(key)} value={form[field]?.[tab]||""} onChange={e=>loc(field,e.target.value)}/>}</label><LanguageGuardNotice locale={locale} language={tab} value={form[field]?.[tab]||""} disabled={submitting||translating} englishAccepted={englishGuardAccepted} onAcceptEnglish={()=>{setEnglishGuardAccepted(true);autoTranslationAttempt.current=""}} onChange={value=>loc(field,value)}/></div>})}
    <div className="admin-panel"><h3>{text.avatar}</h3><div className="admin-avatar-editor"><div><div className={`admin-avatar-crop ${preview?"":"admin-avatar-placeholder"} ${preview&&zoom>100?"is-draggable":""}`} onPointerDown={dragStart} onPointerMove={dragMove} onPointerUp={dragEnd} onPointerCancel={dragEnd}>{preview?<img ref={cropImageRef} className="admin-avatar-crop-image" src={preview} alt="" draggable={false} style={{transform:`scale(${zoom/100})`,transformOrigin:`${focalX}% ${focalY}%`}}/>:form.initials||"?"}</div>{preview&&<div className="admin-avatar-source"><span>{avatarText.fullPreview}</span><img src={preview} alt=""/></div>}</div><div className="admin-avatar-tools"><div className={`admin-image-intake${photoDragActive?" is-dragging":""}`} data-team-image-intake tabIndex={0} role="group" aria-label={intakeText.prompt} onDragEnter={event=>{event.preventDefault();if(transferHasFiles(event.dataTransfer))setPhotoDragActive(true)}} onDragOver={event=>{event.preventDefault();if(transferHasFiles(event.dataTransfer))setPhotoDragActive(true)}} onDragLeave={()=>setPhotoDragActive(false)} onDrop={event=>{event.preventDefault();setPhotoDragActive(false);void receivePhotoTransfer(event.dataTransfer)}}><span className="admin-image-intake-icon" aria-hidden="true">📷</span><strong>{intakeText.prompt}</strong><small>{intakeText.formats}</small><div className="admin-avatar-actions"><label className="admin-button secondary">{text.choose}<input hidden disabled={submitting||translating} type="file" accept="image/png,image/jpeg,image/webp" onChange={e=>{pick(e.target.files?.[0]);e.currentTarget.value=""}}/></label>{preview&&<button type="button" className="admin-button danger" disabled={submitting||translating} onClick={deletePhoto}>{saveStage==="delete-photo"?avatarText.deleting:file?avatarText.removeSelected:avatarText.remove}</button>}</div></div><p className="admin-muted">{text.photoHelp}</p></div></div>
     {photoNotice&&<p className="admin-alert admin-success" role="status">{photoNotice}</p>}
    </div>
    <div className="admin-form-footer"><button className="admin-button secondary" disabled={submitting||translating} onClick={close}>{text.cancel}</button><button className="admin-button secondary" disabled={submitting||translating||external} onClick={()=>save(false)}>{translating?translateText.preparing:submitting?text.saving:autoTranslate&&!translationReady?translateText.prepare:text.save}</button><button className="admin-button primary" disabled={submitting||translating||external} onClick={()=>save(true)}>{translating?translateText.preparing:submitting?text.saving:autoTranslate&&!translationReady?translateText.prepare:text.publish}</button></div>
   </div>
  </aside></>}
 </section>
}
